#include <stdio.h>
#include <stdlib.h>

int main(){
  int xd;
  printf("Ingrese 4 valores: ");
  int valortotal = 0;
  for (int i = 0; i < 4; i++){
  scanf("%d", &xd);
  valortotal = xd + valortotal;      
  }
    printf("su valor es: %d", valortotal / 4);
}
